## Verification

- Unit Tests:
  - `cargo test --workspace`
  - `octopus-execution` unit tests cover deterministic builtin execution and enforce that `ConnectorCall` must go through the gateway path.
- Integration Tests:
  - `cargo test -p octopus-runtime --test slice5_mcp_gateway`
  - Covers connector-backed task success, approval-resume on `manual_event` automation, retryable connector failure normalization, lease heartbeat/release API, stale-lease expiry on reopen, and low-trust knowledge gating.
- Contract Tests:
  - `cargo test -p octopus-runtime --test schema_contracts refined_slice1_examples_validate`
  - Updated examples validate the Slice 5 task/automation connector action, capability descriptor metadata, environment lease states, artifact provenance fields, and knowledge-candidate provenance fields.
- Failure Cases:
  - Fake MCP tool `fail_once_then_emit_text` fails on attempt 1, persists a failed invocation, and recovers through the existing run retry path on attempt 2.
  - Low-trust connector output persists as an artifact and invocation but does not create a `KnowledgeCandidate`.
- Boundary Cases:
  - Stale `active` environment leases are expired automatically when the runtime reopens after migrations.
  - Approval gating remains in front of connector execution, so no invocation is persisted before approval is granted.
- Manual Verification:
  - None planned beyond automated test evidence.
- Static Checks:
  - `cargo fmt --all --check`
- Remaining Gaps:
  - Verification only proves the fake/test-double MCP boundary. It does not prove real transport, credentials, remote hub connectivity, or surface-layer consumers.
- Confidence Level:
  - High for the local SQLite-backed Slice 5 scope that is tracked in this repository.
