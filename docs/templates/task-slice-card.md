# Task Slice Card Template

> 用途：在任何非 trivial 文档、设计、骨架、contract、实现或评审任务开始前，先把任务压缩成最小可验证切片。

---

## 1. 基本信息

- `task_name`:
- `request_type`: `doc | design | contract | skeleton | implementation | review`
- `owner`:
- `date`:

## 2. 目标

- 本次要解决什么问题：
- 为什么现在做：

## 3. 作用范围

- `release_slice`: `GA | Beta | Later`
- `planes`:
- `surfaces`:
- `affected_objects`:

## 4. 边界检查

- 是否会改变产品范围：`yes/no`
- 是否会改变架构主决策：`yes/no`
- 是否会新增平台表面：`yes/no`
- 是否涉及高风险能力或安全姿态变化：`yes/no`
- 是否需要人工确认：`yes/no`

若任一项为 `yes`，写明原因：

-

## 5. 前置产物判断

- 是否需要 `ADR`：
- 是否需要 `contract`：
- 是否需要 `implementation plan`：
- 是否需要视觉/IA 设计：
- 是否需要骨架设计说明：

## 6. 验收条件

- 成功条件 1：
- 成功条件 2：
- 成功条件 3：

## 7. 验证方式

- 当前仓库可实际执行的验证：
- 不能声称执行的验证：

## 8. 风险与停机点

- 主要风险：
- 发现以下情况时必须停下：

## 9. 输出物

- 预计新增或修改的文档/文件：
- 最终汇报需要说明的重点：
