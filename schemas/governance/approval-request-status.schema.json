{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://octopus.local/schemas/governance/approval-request-status.schema.json",
  "title": "ApprovalRequestStatus",
  "type": "string",
  "description": "Minimal GA approval-request status set preserving approval, rejection, expiry, and cancellation semantics.",
  "enum": [
    "pending",
    "approved",
    "rejected",
    "expired",
    "cancelled"
  ]
}
