# Contract Change

- Change Type:
  - Shared Schema
  - Cross-language Contract
  - Internal Interface
  - Persistence Model
- New / Updated Schemas:
  - Update `schemas/interop/hub-login-response.schema.json` to add:
    - `refresh_token`
    - `refresh_expires_at`
  - Add `schemas/interop/hub-refresh-command.schema.json`
  - Add `schemas/interop/hub-refresh-response.schema.json`
- New / Updated Commands:
  - New shared command DTO: `HubRefreshCommand`
- New / Updated Queries:
  - None
- New / Updated Events:
  - None
- New / Updated DTOs:
  - `HubLoginResponse`
  - `HubRefreshCommand`
  - `HubRefreshResponse`
  - `RemoteHubAuthClient.refreshSession(): Promise<HubRefreshResponse>`
  - Desktop-local persisted remote-session cache adds:
    - `refreshToken`
    - `refreshTokenExpiresAt`
- Compatibility Impact:
  - Remote auth consumers must accept the new login response shape.
  - Existing `HubConnectionStatus.auth_state` remains unchanged.
  - Protected remote routes remain access-token only.
- Affected Consumers:
  - `packages/schema-ts`
  - `packages/hub-client`
  - `apps/desktop`
  - `apps/desktop/src-tauri`
  - `apps/remote-hub`
  - `crates/access-auth`
- Migration Notes:
  - Add new SQLite auth-refresh persistence objects and execute them idempotently on service open.
  - Existing session rows remain valid; refresh capability applies to new and refreshed logins after migration.
- Generation Impact:
  - `packages/schema-ts` must register and export the new schemas, parsers, and types.
- Open Questions:
  - None for this slice; broader account/session administration remains future work.
