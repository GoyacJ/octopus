{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://octopus.local/schemas/context/project.schema.json",
  "title": "Project",
  "type": "object",
  "description": "GA foundation placeholder shared contract for Project. Freezes Project as a Workspace-scoped business context boundary.",
  "properties": {
    "id": { "type": "string" },
    "workspace_id": { "type": "string" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  },
  "required": ["id", "workspace_id"],
  "additionalProperties": true
}
