## Verification

- Unit Tests:
  - `cargo test -p octopus-runtime --test ga_minimum_automation_surface`
- Integration Tests:
  - `cargo test -p octopus-remote-hub --test auth_surface automation_routes_require_authentication_and_enforce_workspace_membership`
  - `cargo test -p octopus-remote-hub --test http_surface automation_manager_surface_round_trip_matches_minimum_contracts`
  - `cargo test -p octopus-remote-hub`
- Contract Tests:
  - `cargo test -p octopus-runtime --test schema_contracts`
  - `pnpm --filter @octopus/schema-ts test`
  - `pnpm --filter @octopus/hub-client test`
- Failure Cases:
  - Runtime tests cover invalid lifecycle transitions, manual dispatch rejection for non-manual triggers, and derived recent-delivery/run summaries.
  - Remote-hub tests cover auth failures, workspace membership rejection, and one-time webhook secret reveal.
  - Desktop tests cover lifecycle errors, retrying a non-failed delivery, token-expired read-only mode, and schema validation failures.
- Boundary Cases:
  - Four trigger kinds are covered end-to-end for create/list/detail.
  - Lifecycle boundaries are limited to `active -> paused -> active` and `active/paused -> archived`.
  - Retry remains bounded to previously failed deliveries by reusing runtime semantics.
- Manual Verification:
  - No separate manual UI walkthrough was run outside the automated desktop happy-path suite.
- Static Checks:
  - `pnpm --filter @octopus/desktop typecheck`
- Remaining Gaps:
  - The tracked tree still does not contain a concrete local `window.__OCTOPUS_LOCAL_HUB__` bridge implementation; desktop coverage verifies the client-side surface against mocked local transport and the real remote-hub surface.
- Confidence Level:
  - Moderate to high for the tracked slice because runtime, contracts, remote-hub, client parity, desktop tests, and desktop typecheck were all re-run successfully.
