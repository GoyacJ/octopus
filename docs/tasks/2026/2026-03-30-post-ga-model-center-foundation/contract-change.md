# Contract Change

- Change Type:
  - Shared Schema
  - Cross-language Contract
  - Documentation Alignment
- New / Updated Schemas:
  - Add `schemas/governance/model-provider.schema.json`
  - Add `schemas/governance/model-catalog-item.schema.json`
  - Add `schemas/governance/model-profile.schema.json`
  - Add `schemas/governance/tenant-model-policy.schema.json`
  - Add `schemas/governance/model-selection-decision.schema.json`
- New / Updated Commands:
  - None
- New / Updated Queries:
  - None
- New / Updated Events:
  - None
- New / Updated DTOs:
  - `ModelProvider`
  - `ModelCatalogItem`
  - `ModelProfile`
  - `TenantModelPolicy`
  - `ModelSelectionDecision`
- Compatibility Impact:
  - Additive only. No existing GA schema shape changes in this slice.
  - Canonical overlapping terminology now follows PRD terms: `ModelCatalogItem` replaces supplement-only `ModelCatalogEntry`, and `TenantModelPolicy` replaces supplement-only `ModelAccessPolicy` for first-slice tracked contracts.
- Affected Consumers:
  - `packages/schema-ts`
  - Future Rust runtime/governance consumers
  - Future TypeScript hub/surface consumers
- Migration Notes:
  - No persistence or transport migration in this slice.
  - Runtime storage, policy evaluation, and provider adapters remain future work.
- Generation Impact:
  - `packages/schema-ts` registers and exports the new schema parsers and TypeScript interfaces.
- Open Questions:
  - Whether later slices need separate `ModelFeatureSet`, `ProviderEndpointProfile`, `ModelRoutingPolicy`, `ProviderBuiltInTool*`, or adapter SPI contracts remains deferred.
