## Contract Change

- Change Type:
  - Public API
  - Internal Interface
  - Cross-language Contract
- New / Updated Schemas:
  - None. Reuse:
    - `schemas/context/project.schema.json`
    - `schemas/interop/local-hub-transport.json`
    - `schemas/interop/local-hub-transport.schema.json`
- New / Updated Commands:
  - Add one interop-owned local transport command: `list_projects`
- New / Updated Queries:
  - Add one shared read method: `HubClient.listProjects(workspaceId): Promise<Project[]>`
- New / Updated Events:
  - None.
- New / Updated DTOs:
  - Reuse the existing shared `Project` DTO for list items.
  - Extend the desktop-local connection profile type with optional non-secret `projectId`.
- Compatibility Impact:
  - Additive. Existing login/session DTOs and project-scoped workbench consumers remain unchanged.
- Affected Consumers:
  - `schemas/interop`
  - `packages/schema-ts`
  - `packages/hub-client`
  - `crates/domain-context`
  - `crates/runtime`
  - `apps/remote-hub`
  - `apps/desktop/src-tauri`
  - `apps/desktop`
- Migration Notes:
  - No Rust persistence migration is expected.
  - Desktop local storage gains one additive optional `projectId` field on the connection profile record.
- Generation Impact:
  - None. Existing schema-ts generation and parser patterns are sufficient.
- Open Questions:
  - None at task freeze time. Workspace discovery or project CRUD must remain separate future slices.
