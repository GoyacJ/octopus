## Verification

- Unit Tests:
  - `pnpm --filter @octopus/schema-ts test`
  - `pnpm --filter @octopus/hub-client test`
  - `pnpm --filter @octopus/desktop test`
- Integration Tests:
  - `cargo test -p octopus-runtime --test slice12_governance_explainability -- --nocapture`
  - `cargo test -p octopus-remote-hub --test http_surface -- --nocapture`
- Contract Tests:
  - `packages/schema-ts/test/contracts.test.ts`
  - `crates/runtime/tests/schema_contracts.rs`
  - `packages/hub-client/test/hub-client.contract.test.ts`
- Failure Cases:
  - Verified `grant missing`, `budget missing`, `soft limit`, `hard limit`, and `high risk` capability resolution branches in Rust.
  - Verified remote-hub rejects invalid negative `estimated_cost` through the existing HTTP surface tests.
- Boundary Cases:
  - Verified Workspace explainability updates when task estimated cost changes from executable to approval-required.
  - Verified Run view consumes existing `policy_decisions` without changing the wire shape.
- Manual Verification:
  - None beyond automated test coverage.
- Static Checks:
  - `pnpm typecheck:ts`
- Remaining Gaps:
  - No dedicated UI editing path exists for grants or budgets.
  - Capability explainability remains intentionally limited to project-bound capabilities.
- Confidence Level:
  - High after focused slice tests and full workspace verification.
