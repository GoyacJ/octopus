Octopus `v0.1.0` establishes the first formal desktop release line.

- ships the desktop shell with the Rust sidecar packaged through the Tauri release workflow
- enforces full-repository quality gates across frontend, Rust workspace, schema governance, and version governance
- publishes verified macOS and Windows installers with generated release checksums and canonical contract metadata
