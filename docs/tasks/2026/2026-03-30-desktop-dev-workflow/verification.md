## Verification

- Unit Tests:
  - `pnpm --filter @octopus/desktop exec vitest run test/launch-config.test.ts`
    - Passed.
    - Covers desktop package scripts, root dev scripts, `frontendDist`, `beforeDevCommand`, `beforeBuildCommand`, and `devUrl`.
  - `node --test test/scripts/dev-workflows.test.mjs`
    - Passed.
    - Covers remote-hub dev env isolation, combined workflow specs, and manual login instructions.
  - `cargo test -p octopus-remote-hub dev_seed`
    - Passed.
    - Covers empty-DB dev seed login and project discovery for `workspace-alpha`.
- Integration Tests:
  - `pnpm --filter @octopus/desktop test`
    - Passed.
  - `cargo test -p octopus-remote-hub`
    - Passed.
- Contract Tests:
  - No shared-contract changes; only config/script assertions plus app-local remote-hub seeding coverage.
- Failure Cases:
  - The red phase caught the missing `scripts/dev/workflows.mjs` module and missing remote-hub dev seed export before implementation.
  - Manual smoke caught that Tauri executes `beforeDevCommand` from `apps/desktop`, which required changing the config from `pnpm --dir .. ...` to `pnpm run ...`.
- Boundary Cases:
  - Remote-hub dev seed is scoped to the isolated dev DB and guarded by `OCTOPUS_REMOTE_HUB_DEV_SEED=1`.
  - Stable `desktop:open` and `remote-hub:start` script definitions remain unchanged and are still asserted in the desktop config regression test.
- Manual Verification:
  - `pnpm remote-hub:dev`
    - Passed as a startup smoke.
    - Confirmed the script uses `target/dev/remote-hub.sqlite`.
    - Confirmed `/api/hub/connection` reports `auth_required`.
    - Confirmed manual login with `workspace-alpha / admin@octopus.local / octopus-bootstrap-password`.
    - Confirmed project listing returns `project-remote-demo`.
  - `pnpm desktop:dev:remote`
    - Passed as a startup smoke.
    - Confirmed prefixed logs for both child processes.
    - Confirmed the manual login instructions print once remote-hub is ready.
    - Confirmed Vite dev server starts on `http://127.0.0.1:5173`.
    - Confirmed Tauri dev host reaches the native `octopus-desktop-host` run step before shutdown.
- Static Checks:
  - `pnpm --filter @octopus/desktop typecheck`
    - Passed.
  - `pnpm --filter @octopus/desktop exec tauri --version`
    - Passed.
    - Reported `tauri-cli 2.10.1`.
- Remaining Gaps:
  - Stable `pnpm desktop:open` and `pnpm remote-hub:start` were not re-run during this task because they target the non-isolated user/default runtime paths.
  - A live in-window Vue file edit was not exercised, so HMR correctness is inferred from the verified Vite + Tauri dev startup chain rather than a GUI automation assertion.
- Confidence Level:
  - High.
