## Contract Change

- Change Type: Cross-language Contract
- New / Updated Schemas:
  - New placeholder JSON Schemas under `schemas/context/`
  - New placeholder JSON Schemas under `schemas/runtime/`
  - New placeholder JSON Schemas under `schemas/governance/`
  - New placeholder JSON Schemas under `schemas/observe/`
  - New strong state-enum schemas for `Run`, `ApprovalRequest`, `TriggerDelivery`, `EnvironmentLease`, `InboxItem`, and `Notification`
- New / Updated Commands: None
- New / Updated Queries: None
- New / Updated Events: None
- New / Updated DTOs:
  - Placeholder shared DTO-shaped objects for first-priority GA object names and ownership groups
- Compatibility Impact:
  - No runtime consumer compatibility impact yet because no tracked runtime consumers exist.
  - This task freezes naming, placement, `$id` pattern, and minimal cross-language semantics for later slices.
- Affected Consumers:
  - Future Rust consumers under `crates/`
  - Future TypeScript consumers under `packages/`
  - Future surface assembly in `apps/`
- Migration Notes:
  - None in this phase.
  - Later slices must refine placeholder schemas instead of replacing them with parallel contract truth elsewhere.
- Generation Impact:
  - Generation is explicitly deferred.
  - Later codegen or wrapper layers must treat these JSON Schemas as upstream source, not as generated outputs.
- Open Questions:
  - Which concrete generation toolchain will derive Rust and TypeScript consumers.
  - How command/query/event families should be grouped once `Slice 1` freezes actual field sets.
