## Contract Change

- Change Type:
  - Public API
  - Internal Interface
  - Cross-language Contract
- New / Updated Schemas:
  - None. Reuse:
    - `schemas/interop/hub-login-command.schema.json`
    - `schemas/interop/hub-login-response.schema.json`
    - `schemas/interop/hub-session.schema.json`
    - `schemas/interop/hub-auth-error.schema.json`
- New / Updated Commands:
  - None in `schemas/interop`.
- New / Updated Queries:
  - New auth-only TypeScript client method: `RemoteHubAuthClient.getCurrentSession()`
- New / Updated Events:
  - None.
- New / Updated DTOs:
  - New TypeScript interface only: `RemoteHubAuthClient`
  - New desktop-local profile type for persisted non-secret connection state
  - Reused DTOs: `HubLoginCommand`, `HubLoginResponse`, `HubSession`, `HubAuthError`
- Compatibility Impact:
  - Additive. Existing `HubClient` task/run/automation/knowledge/governance consumers remain unchanged.
- Affected Consumers:
  - `packages/hub-client`
  - `apps/desktop`
- Migration Notes:
  - No persistence migration is expected in Rust.
  - Desktop local storage gains one new app-local connection profile record.
- Generation Impact:
  - None. No schema generation changes are required.
- Open Questions:
  - None at task freeze time. If remote bootstrap later needs secure token persistence, that must land in a separate slice instead of expanding Slice 16.
