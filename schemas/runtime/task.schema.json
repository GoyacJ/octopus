{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://octopus.local/schemas/runtime/task.schema.json",
  "title": "Task",
  "type": "object",
  "description": "Slice 1 shared contract for Task as the user-facing business object that enters the formal Run system.",
  "$defs": {
    "emit_text_action": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "emit_text"
        },
        "content": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": ["kind", "content"],
      "additionalProperties": false
    },
    "fail_once_then_emit_text_action": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "fail_once_then_emit_text"
        },
        "failure_message": {
          "type": "string",
          "minLength": 1
        },
        "content": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": ["kind", "failure_message", "content"],
      "additionalProperties": false
    },
    "always_fail_action": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "always_fail"
        },
        "message": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": ["kind", "message"],
      "additionalProperties": false
    }
  },
  "properties": {
    "id": { "type": "string" },
    "workspace_id": { "type": "string" },
    "project_id": { "type": "string" },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "instruction": {
      "type": "string",
      "minLength": 1
    },
    "action": {
      "oneOf": [
        { "$ref": "#/$defs/emit_text_action" },
        { "$ref": "#/$defs/fail_once_then_emit_text_action" },
        { "$ref": "#/$defs/always_fail_action" }
      ]
    },
    "capability_id": {
      "type": "string",
      "minLength": 1
    },
    "estimated_cost": {
      "type": "integer",
      "minimum": 0
    },
    "idempotency_key": {
      "type": "string",
      "minLength": 1
    },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  },
  "required": [
    "id",
    "workspace_id",
    "project_id",
    "title",
    "instruction",
    "action",
    "capability_id",
    "estimated_cost",
    "idempotency_key",
    "created_at",
    "updated_at"
  ],
  "additionalProperties": false
}
