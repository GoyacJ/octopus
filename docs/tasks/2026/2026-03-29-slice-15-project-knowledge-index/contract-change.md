## Contract Change

- Change Type:
  - Cross-language Contract
- New / Updated Schemas:
  - New: `schemas/observe/project-knowledge-index.schema.json`
  - Updated: `schemas/interop/local-hub-transport.json`
  - Updated: `schemas/interop/local-hub-transport.schema.json`
- New / Updated Commands:
  - New local transport command: `get_project_knowledge`
- New / Updated Queries:
  - New shared client query: `HubClient.getProjectKnowledge(workspaceId, projectId)`
  - New remote read route: `GET /api/workspaces/:workspaceId/projects/:projectId/knowledge`
- New / Updated Events:
  - None. Existing events may only trigger refetch behavior.
- New / Updated DTOs:
  - New DTO: `ProjectKnowledgeIndex { knowledge_space, entries }`
  - Reused DTO: `KnowledgeSummary` for the mixed entry list
  - Unchanged DTO: `KnowledgeDetail` remains run-scoped
- Compatibility Impact:
  - Additive. Existing run-scoped knowledge detail consumers remain unchanged.
- Affected Consumers:
  - `packages/schema-ts`
  - `packages/hub-client`
  - `apps/desktop`
  - `apps/desktop/src-tauri`
  - `apps/remote-hub`
  - `crates/runtime`
- Migration Notes:
  - No persistence migration is expected.
  - Transport consumers must add the new read command / route wiring before desktop can consume the page.
- Generation Impact:
  - Schema registry and TypeScript parsers must be updated to include `ProjectKnowledgeIndex`.
- Open Questions:
  - None at task start. If existing `KnowledgeSummary` proves insufficient for traceability, the first fallback is entry correlation using existing source refs before any schema expansion.
