{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://octopus.local/schemas/context/knowledge-space.schema.json",
  "title": "KnowledgeSpace",
  "type": "object",
  "description": "GA foundation placeholder shared contract for KnowledgeSpace. Freezes the authority container for shared knowledge without final field design.",
  "properties": {
    "id": { "type": "string" },
    "workspace_id": { "type": "string" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  },
  "required": ["id", "workspace_id"],
  "additionalProperties": true
}
