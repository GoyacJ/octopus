{
  "name": "@octopus/ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "vue-tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "build": "vue-tsc -p tsconfig.build.json --declaration --emitDeclarationOnly"
  },
  "dependencies": {
    "vue": "catalog:"
  },
  "devDependencies": {
    "@octopus/tsconfig": "workspace:*"
  }
}
