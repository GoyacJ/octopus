## Verification

- Unit Tests:
  - `cargo test -p octopus-runtime --test schema_contracts`
  - `cargo test -p octopus-runtime --test slice2_governance`
  - `cargo test -p octopus-runtime --test slice11_governance_surface`
- Integration Tests:
  - `cargo test -p octopus-remote-hub --test auth_surface`
  - `cargo test -p octopus-remote-hub --test http_surface`
- Contract Tests:
  - `pnpm --filter @octopus/schema-ts test`
  - `pnpm --filter @octopus/hub-client test`
- Desktop Tests:
  - `pnpm --filter @octopus/desktop test`
- Static Checks:
  - `pnpm --filter @octopus/desktop typecheck`
- Failure Cases:
  - Runtime tests cover idempotent promotion requests, approval branching, knowledge-asset creation on approval, rejection behavior, and execution-approval regression.
  - Remote-hub tests cover authentication, workspace membership enforcement, approval detail retrieval, request-promotion, and resolve round-trips for both approval types.
  - Desktop tests cover workspace inline governance actions, run-view promotion requests, approval resolution, and token-expired read-only disabling.
- Boundary Cases:
  - `target_ref` is backfilled for existing execution approvals and propagated to inbox / notification records.
  - A `KnowledgeCandidate` reuses the same open pending promotion approval instead of creating duplicates.
  - `knowledge_promotion` approval changes candidate state only on approval and leaves Run state unchanged on all other outcomes.
- Manual Verification:
  - No separate manual UI walkthrough was run outside the automated desktop suite.
- Remaining Gaps:
  - The tracked tree still does not contain a concrete local `window.__OCTOPUS_LOCAL_HUB__` bridge implementation; desktop coverage verifies the client-side surface against mocked local transport and the real remote-hub surface.
- Confidence Level:
  - Moderate to high once the runtime, remote-hub, schema-ts, hub-client, desktop, and desktop typecheck suites all pass in one final verification run.
